import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#045681', // Customize the primary color
    },
    secondary: {
      main: '#98bccc', // Customize the secondary color
    },
  },
});

export default theme;

